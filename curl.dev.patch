diff --git a/lib/Makefile.m32 b/lib/Makefile.m32
index 325cdc7af..21ff31c8b 100644
--- a/lib/Makefile.m32
+++ b/lib/Makefile.m32
@@ -289,7 +289,7 @@ ifdef ZLIB
   DLL_LIBS += -L"$(ZLIB_PATH)" -lz
 endif
 ifdef BROTLI
-  INCLUDES += -I"$(BROTLI_PATH)"
+  INCLUDES += -I"$(BROTLI_PATH)/include"
   CFLAGS += -DHAVE_BROTLI
   DLL_LIBS += -L"$(BROTLI_PATH)/lib" -lbrotlidec
 endif
diff --git a/src/Makefile.m32 b/src/Makefile.m32
index ffc359149..e17aadd19 100644
--- a/src/Makefile.m32
+++ b/src/Makefile.m32
@@ -303,7 +303,7 @@ ifdef ZLIB
   curl_LDADD += -L"$(ZLIB_PATH)" -lz
 endif
 ifdef BROTLI
-  INCLUDES += -I"$(BROTLI_PATH)"
+  INCLUDES += -I"$(BROTLI_PATH)/include"
   CFLAGS += -DHAVE_BROTLI
   curl_LDADD += -L"$(BROTLI_PATH)/lib" -lbrotlidec
 endif
